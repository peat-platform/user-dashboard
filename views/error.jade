extends new_layout

block content
    .error-template.alert.alert-danger(role="alert")
        strong Oops! Some error happens. Please try to reconnect in a few minutes.
        br
        br
        h5= error
    //h2= error.status
    //pre #{error.stack}
